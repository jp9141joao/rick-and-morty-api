export type Login = {
    email: string,
    senha: string
}

export type Usuario = {
    nome: string,
    email: string,
    senha: string
}

export type MensagemToast = {
    variant: string,
    title: string,
    description: string,
}